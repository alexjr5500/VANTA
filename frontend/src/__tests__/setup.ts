// VANTA frontend test setup (vitest). Kept intentionally minimal: media/URL
// unit tests run in the `node` environment (see the per-file
// `@vitest-environment node` doc-comments) and do not require a DOM.